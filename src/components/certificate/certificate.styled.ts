import { StyleSheet } from '@react-pdf/renderer';

export const styles = StyleSheet.create({
  image: {
    top: 60,
    width: '100%',
    position: 'absolute',
  },
  text: {
    width: '60%',
    left: '20%',
    top: '40%',
    textAlign: 'center',
    position: 'absolute',
  },
});
