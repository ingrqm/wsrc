export enum Align {
  left = 'left',
  center = 'center',
  right = 'right',
  justify = 'justify',
}

export enum AlignFlex {
  start = 'start',
  center = 'center',
  end = 'end',
  justify = 'justify',
  between = 'space-between',
}
